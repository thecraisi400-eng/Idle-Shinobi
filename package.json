{
  "name": "idle-shinobi",
  "version": "0.2.0",
  "private": true,
  "description": "Idle Shinobi — juego web 100% offline: sprites embebidos en el HTML y service worker opcional.",
  "scripts": {
    "build": "python3 tools/build.py",
    "icons": "python3 tools/make-icons.py",
    "test": "node test/offline.test.js",
    "start": "python3 -m http.server 8080 --bind 0.0.0.0"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
